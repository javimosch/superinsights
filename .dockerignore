# Keep secrets, VCS, runtime data and bulk out of the Docker build context so
# they can never be baked into the image (esp. once `COPY . .` is restored).
.env
.env.*
*.env
.git
.gitignore
node_modules
data
uploads
*.log
ref-saasbackend
docs
examples
.github
test
*.md
